// Project convention: this system only ever talks to one Multica workspace.
// All Multica queries must use this slug — never pass project slugs or other workspace names.
export const MULTICA_WORKSPACE_SLUG = 'gtm'
