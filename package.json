{
  "name": "gtm-swarm",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd dashboard && pnpm install --frozen-lockfile && pnpm build",
    "start": "node server/index.js",
    "dev": "cd dashboard && pnpm dev"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.1",
    "express": "^4.21.2",
    "gray-matter": "^4.0.3",
    "js-yaml": "^4.1.0"
  }
}
